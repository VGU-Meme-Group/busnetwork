pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scmGit(branches: [[name: '*/MBNP-24-machine-learning']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/VGU-Meme-Group/busnetwork']])
            }
        }

        stage('Build') {
            steps {
                
            }
        }

        stage()
    }
    
}
